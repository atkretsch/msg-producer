environments:
- name: acceptance
  provider: ecs-fargate
- name: production
  provider: ecs-fargate
service:
  networkMode: awsvpc
  port: 80
  pathPatterns:
  - /msg-producer/*
  pipeline:
    source:
      provider: GitHub
      repo: atkretsch/msg-producer
templates:
  mu-iam-service-msg-producer-acceptance:
    Resources:
      EcsTaskRole:
        Properties:
          ManagedPolicyArns:
          - "arn:aws:iam::aws:policy/AmazonSQSFullAccess"
